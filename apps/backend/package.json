{
  "name": "backend",
  "scripts": {
    "build": "bun build src/index.ts --output=dist --target=browser --format=esm",
    "deploy": "cd ../.. && bunx wrangler deploy --config apps/backend/wrangler.jsonc",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@repo/types": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "hono": "^4.11.7",
    "short-unique-id": "^5.3.2",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260408.1",
    "@types/bun": "latest",
    "wrangler": "^4.81.0"
  }
}

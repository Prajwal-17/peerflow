export const pcRef = { current: null as RTCPeerConnection | null };
export const socketRef = { current: null as WebSocket | null };
export const roomIdRef = { current: null as string | null };
export const dcRef = { current: null as RTCDataChannel | null };
export const ctrlRef = { current: null as RTCDataChannel | null };
